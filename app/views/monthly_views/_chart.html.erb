$('#<%= to %>').highcharts({
  chart : {
    height: 600
  },

  title : {
    text : null
  },

  xAxis : {
    type : 'datetime',
    title : {
      text : 'Views'
    },
    categories: [
      <% day = @start_day %>
      <% while day <= @end_day %>
        '<%= day.strftime('%b %Y') %>',
        <% day += 1.month %>
      <% end %>
    ]
  },

  yAxis : {
    title : {
      text : 'Views'
    }
  },

  rangeSelector: {
    selected : 3
  },

  navigator : {
    enabled : false
  },

  plotOptions : {
    column : {
      stacking : 'normal'
    }
  },

  series : [
    <% chart_data.each do |country, data| %>
      {
        name : '<%= country %>',
        data : <%= data %>,
        type : 'column',
        stack: 1
      },
    <% end %>
  ]
});
